groups:
- name: Requests number
  rules:
  - alert: ManyRequests
    expr: rate(application_httprequests_transactions_count{env="$environment",app="$application",server="$server"}[1m])*60 > 2
    for: 1m
    labels:
      severity: page
    annotations:
      summary: High requests