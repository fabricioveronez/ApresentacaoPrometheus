groups:
- name: Requests number
  rules:
  - alert: ManyRequests
    expr: rate(application_httprequests_transactions_count{app="CoreCRUD.Api"}[1m])*60 > 2    
    labels:
      severity: page
    annotations:
      summary: High requests